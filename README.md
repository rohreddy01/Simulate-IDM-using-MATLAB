# Simulate-IDM-using-MATLAB
This project includes a ready to run MATLAB script that simulates the ego and lead vehicle, computes the acceleration from IDM, computes Time-To-Collision (TTC),runs a parameter-sweep (sensitivity) over key parameters and saves the result
